declare global {
  namespace ReactNativePaper {
    interface ThemeColors {
      available: {
        background: string;
        text: string;
      };
      black: string;
      border: string;
      gray: string;
      failure: {
        background: string;
        text: string;
      };
      red: string;
      success: {
        background: string;
        disabled: string;
        text: string;
      };
      unset: {
        background: string;
        text: string;
      };
      white: string;
      wrong: {
        background: string;
        text: string;
      };
    }
  }
}

export {};
