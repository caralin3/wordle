declare global {
  namespace ReactNativePaper {
    interface ThemeColors {
      available: {
        background: string;
        text: string;
      };
      gray: string;
      failure: {
        background: string;
        text: string;
      };
      success: {
        background: string;
        disabled: string;
        text: string;
      };
      unset: {
        background: string;
        text: string;
      };
      wrong: {
        background: string;
        text: string;
      };
    }
  }
}

export {};
